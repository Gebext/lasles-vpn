const PricingButton = () => {
  return (
    <div>
      <button className="border-2 border-primary text-primary px-12 py-2 rounded-full font-bold hover:bg-primary hover:text-white">
        Select
      </button>
    </div>
  );
};

export default PricingButton;
